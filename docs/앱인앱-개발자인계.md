# 드림그룹 앱인앱(웹뷰) 연동 — 개발자 인계 자료

> 목적: 애돌(최애돌) 앱 안에 **드림그룹을 앱인앱(웹뷰) 게임**으로 얹을 때,
> ① 로그인 유저만 진입시키는 **허들(인증)** 과 ② 그 유저의 **최애 아이돌 정보**를 웹게임에 넘기는 방법.
> 애돌 **본체 코드/DB/동선은 건드리지 않고**, 기존에 이미 깔려 있는 길을 재활용하는 것이 원칙입니다.

작성일 2026-07-27 · 소스 기준 커밋: idol_ios `d4e805da6` / idol_android2 `b6125289` / idol_realserver_source `0e3cad7e2`

> 2026-08-03 웹게임 구현 갱신: `product-telemetry.js`에 앱↔웹 메시지 계약을 먼저 넣었습니다. 현행 네이티브는 아래 새 타입을 아직 무시하므로, **앱인앱 리텐션 베타 전 네이티브 수신과 서버 적재를 반드시 연결해야 합니다.** 정적 웹 단독 배포는 UI 데모일 뿐, 인증된 유저별 D1/D2 측정판이 아닙니다.

## 0-A. 드림그룹 웹 메시지 계약 (2026-08-03)

웹 → 앱:

| type | data | 목적 |
|---|---|---|
| `DREAM_GROUP_READY` | `{ version }` | 웹 로드 완료. 앱이 인증 컨텍스트를 내려줄 시점 |
| `DREAM_GROUP_EVENT` | `{ event, session_id, elapsed_ms, slot, ...props }` | 화면·RUN·일일 프로젝트·데뷔·공유 퍼널 적재 |

앱 → 웹 (`window.postMessage`):

| type | data | 목적 |
|---|---|---|
| `DREAM_GROUP_CONTEXT` | `{ favorite_id?, favorite_name?, favorite_group?, locale?, app_version? }` | 첫 선택 UX에 필요한 최소 컨텍스트 전달. 유저 ID·토큰·이메일은 금지 |

필수 이벤트는 `app_open`, `screen_view`, `run_start`, `stage_strategy`, `run_finish`, `daily_complete`, `daily_reply_open`, `group_debut`, `result_share`입니다. `slot`은 진입 URL의 `?slot=` 값만 기록하며 토큰은 URL·localStorage·이벤트 속성에 넣지 않습니다.

웹게임은 검증된 `favorite_id`를 받으면 첫 유저의 아이돌 검색 화면에서 해당 최애를 자동 선택합니다. 웹 브리지는 최애 UX 필드만 허용하며 `user_id`, 토큰, 이메일 등 계정·인증 정보는 폐기합니다.

출시 게이트:

1. 앱이 `DREAM_GROUP_READY`를 받은 뒤 서버 검증이 끝난 컨텍스트만 `DREAM_GROUP_CONTEXT`로 보낸다.
2. iOS/Android 모두 `DREAM_GROUP_EVENT`를 동일 분석 원장에 적재한다.
3. 동일 로그인 테스트 계정으로 D0 진입 → 다음 KST 날짜 D1 재진입이 한 유저로 집계되는지 확인한다.
4. JWT 없음·만료·변조 상태에서는 게임 자산/시즌 API를 거부한다.
5. 계정 귀속 저장과 시즌 결과는 클라이언트 localStorage/직접 Firestore 쓰기가 아니라 서버 권한으로 확정한다.

---

## 0. 요약 — 이미 절반 이상 깔려 있습니다

| 필요한 것 | 현황 | 재활용/신설 |
|---|---|---|
| 앱 안에서 웹게임을 웹뷰로 띄우기 | **iOS·안드 모두 이미 구현** (`GameWebView`/`GamePortal`) | ✅ 재활용 |
| 웹뷰에 유저정보 URL/헤더/JS로 주입 | **이미 함** (닉네임·언어·테마·하트잔액) | ✅ 재활용 |
| 로그인 유저만 통과시키는 인증 | 웹용 **JWT 발급 API `web_token` 이미 존재** | ✅ 재활용 |
| 외부 웹서비스에 유저 신원 안전하게 넘기기 | **C:Z9 SSO 선례 존재** (RS256+JWKS 완성형) | ✅ 복제 |
| 웹게임용 "최애 조회" API | 없음 (기존 self API는 앱 전용 인증) | ⏸️ **선택** (있으면 편의, 없어도 됨) |
| 웹게임용 권한 스코프 | `web:recommend:read`만 있음 | ⏸️ 최애 API 붙일 때만 추가 |

> **범위 결정(2026-07-27):** MVP는 **"로그인 유저만 통과시키는 인증(허들)"만 필수**로 합니다.
> 최애 정보 연동은 **편의성 옵션** — 나중에 얹어도 됩니다. 발급되는 JWT 안에 이미 `sub`(user_id)가 있어
> 유저 식별은 확보되므로, 최애 조회 API는 필요해지는 시점에 추가하면 됩니다.

**기존 부품은 있지만 종단 연결은 아직 필요합니다.** `web_token` 발급은 재활용할 수 있으나, 앱의 `DREAM_GROUP_READY/EVENT` 수신, 검증된 컨텍스트 전달, 계정 귀속 저장·이벤트 서버 적재가 모두 붙고 동일 테스트 계정의 D0→D1이 한 유저로 확인되어야 상용 인앱 판정이 가능합니다.

---

## 1. 전체 그림 (권장 아키텍처)

```
[애돌 앱]                              [드림그룹 웹게임]              [애돌 서버]
   │  ① Basic 인증으로 웹토큰 요청                                       │
   │  GET /api/v1/users/web_token/  ───────────────────────────────────▶│
   │◀──────────────────────  1시간짜리 JWT(aud=web) 반환 ───────────────│
   │                                                                     │
   │  ② JWT를 드림그룹 서버 세션으로 교환(HttpOnly 쿠키)                  │
   │  ③ 웹뷰 오픈 + 허용된 최애 컨텍스트만 전달 ──────▶ [웹게임 로드]      │
   │                                        │                            │
   │                         [드림그룹 API] ◀─ HttpOnly 세션으로 저장·보상 조회
   │                                        └─ 서버 세션 sub로 소유권 확정
```

- **허들(로그인 유저만)** = ②에서 JWT를 검증해 HttpOnly 세션을 만들고, 세션 없는 게임 API 요청은 서버가 거부.
- **최애 몰입 연동** = ③의 컨텍스트로 첫 선택 UX를 구성하되, 저장·보상 소유권은 서버 세션의 `sub`만 신뢰.
- 애돌 본체 경제(하트)·랭킹은 **건드리지 않음** (읽기만, 쓰기 금지).

> 보안: 토큰은 URL·postMessage·웹 JavaScript에 전달하지 않습니다. 네이티브가 HTTPS로 서버 세션과 교환하고, WebView에는 HttpOnly·Secure 쿠키만 적용합니다.

---

## 2. iOS 재활용 지점 (`idol_ios/idolapp/`)

**핵심 파일: `Game/SwiftUI/GameWebViewModel.swift`** — 이미 웹게임 URL 빌드 + 헤더 + JS 브리지 완비.

- 웹게임 URL 파라미터 조립: `buildUrlParameters()` **:135-149** (현재 nickname/flavor/locale/theme)
- 요청 헤더 세팅: `createGameRequest(url:)` **:172-179**
- JS 브리지(`nativeApp`): 설정 **:41-48**, 수신 **:184-212**, 발신(evaluateJavaScript) **:356-365**
- WKWebView 생성: `GameWebView.swift:75-85`

**추가할 것:**
1. `DREAM_GROUP_READY` 수신 시 `AccountManager.shared.user`의 허용 필드만 `DREAM_GROUP_CONTEXT`로 전달
2. `DREAM_GROUP_EVENT`를 허용목록 검증 후 인증 계정의 분석 원장에 적재
3. 기존 `web_token`은 네이티브가 드림그룹 서버와 세션을 교환하는 데만 사용하고, 웹 JS·URL에는 전달하지 않음

**인증 토큰 진입점:** `Api.swift:109 getCredential()` (Basic, `base64(email:token)`) — 토큰 문자열의 유일한 진입점. 최애 읽기: `AccountManager.shared.user?.most` (타입 `IdolData`, 필드 `id`/`name`/`image_url`, `UserData.swift` · `SQLite/IdolData.swift`).

---

## 3. 안드로이드 재활용 지점 (`idol_android2/app/src/main/java/net/ib/mn/`)

**핵심 파일: `presentation/game/portal/GamePortalViewModel.kt`** — iOS와 대칭 구조.

- 웹게임 URL 빌드: `getGameUrl()` **:108-118**, 파라미터 조립 `addParamsToUrl()` **:131-149** (현재 nickname/flavor/locale/theme)
- 웹뷰 화면 + JS 브리지: `presentation/game/webview/GameWebviewScreen.kt`
  - JS→앱 수신 `addJavascriptInterface("Android")` **:275-312**
  - 앱→JS 발신 견본 `sendHeartBalanceToWebView()` **:199-212** (하트잔액을 postMessage로 내려주는 코드 — 토큰/최애도 동일 패턴)
  - URL 로드 **:351**, onPageFinished **:316**

**추가할 것:** `GameWebViewJsInterface`가 `DREAM_GROUP_READY/EVENT`를 수신하게 하고, READY에는 허용된 사용자·최애 컨텍스트를 `window.postMessage`로 응답하며 EVENT는 인증 계정의 분석 원장에 적재합니다. 최애 이름·ID는 컨텍스트에만 넣고 URL 쿼리에 넣지 않습니다. JWT는 네이티브↔드림그룹 서버 세션 교환에만 사용하고 WebView JavaScript에 노출하지 않습니다.

- **인증 토큰:** `data/remote/interceptor/AuthInterceptor.kt:149` — `email:domain:token` 3종을 Base64 Basic. 저장소 `PreferencesManager.kt` (accessToken:541 / loginEmail:546 / loginDomain:551)
- **최애:** `data/repository/UserCacheRepository.kt` (`mostIdolId` 등), 모델 `data/remote/dto/OtherResponses.kt:168 MostIdol` (id / name / imageUrl)

> ⚠️ 인증 형식이 iOS는 `email:token`(2종), 안드는 `email:domain:token`(3종)으로 보입니다. 웹게임 서버는 **이걸 직접 검증하지 말고**, 아래 서버 JWT(`web_token`)를 받아 쓰는 것이 깔끔합니다.

---

## 4. 서버 재활용 지점 (`idol_realserver_source/`) — ★가장 중요

### ✅ 그대로 재활용 (신설 불필요)

**(1) 웹용 JWT 발급 API — 이미 존재**
`GET /api/v1/users/web_token/`
- 리소스 `api/users/resources.py:5260` · 로직 `api/users/facades.py:158`
- 앱이 Basic 인증으로 호출 → **1시간 TTL JWT(aud="web") 발급** (docstring에 "일회성" 명시)
- JWT 구현 `utils/jwts.py` (`create_access_token:43` / `verify_access_token:76`)

**(2) 웹 JWT 검증 데코레이터 — 이미 존재**
`@auth_jwt(scope)` — `api/views_common.py:326` (`_auth_jwt:300`)
- 게임 API 메서드에 이 데코레이터만 붙이면 `request.user` 자동 세팅
- 실사용 예시(그대로 따라 만들 템플릿): `api/recommend/resources.py:50` `@auth_jwt("web:recommend:read")`

**(3) 외부 웹서비스에 유저를 넘기는 완성형 선례 — C:Z9 SSO ★**
`api/auths/services.py Cz9SsoService`
- 최애돌이 **IdP(신원 발급자)**, 외부 서비스가 **검증자**. RS256 서명 id_token + JWKS 공개.
- 발급 `POST /api/v1/auths/sso/cz9_token/` (`api/auths/resources.py:44`)
- 검증용 공개키 `GET /api/v1/auths/jwks/` (`api/auths/resources.py:37`)
- id_token에 nickname·login_provider·profile_image_url·locale 등 포함 (`services.py:56~`)
- **드림그룹이 자체 백엔드를 갖는다면 이 SSO 패턴을 복제하는 것이 가장 안전** (대칭키 공유 회피).

### ⚠️ 신설 필요 (경량)

**(4) 웹게임용 최애 조회 API**
- 기존 `GET /api/v1/users/self/` (`api/users/resources.py:1520`, 최애 전체 반환)는 `@auth_mirror`(앱 Basic 전용)이라 JWT로 못 부름.
- → `@auth_jwt("web:game:read")` 를 단 **경량 신규 엔드포인트**(예: `GET /api/v1/users/most/`) 추가. 내부에서 `request.user.most`만 뽑아 반환.
- DTO/facade 구성은 모범 모듈 `api/recommend/`, `api/charts/` 따라가면 됨.

**(5) 스코프 1개 추가**
- 현재 `web_token`이 주는 scope는 `["web:recommend:read"]` 하나뿐 (`facades.py:164`).
- `web:game:read` 를 여기에 추가하고, (4)의 데코레이터와 짝 맞춤.

---

## 5. 신설 작업 최소 목록 (개발 견적용)

**MVP (필수):**

| # | 작업 | 위치 | 규모 |
|---|---|---|---|
| 1 | iOS: READY/EVENT 수신·안전한 CONTEXT 응답 | `GameWebViewModel.swift` | S |
| 2 | 안드: READY/EVENT 수신·안전한 CONTEXT 응답 | `GameWebviewScreen.kt` | S |
| 3 | 네이티브가 `web_token`으로 드림그룹 서버 세션 교환 | 앱 + 드림그룹 API | M |
| 4 | 계정 귀속 저장·일일 원장·이벤트 적재 | 드림그룹 API | M~L |

**나중에 (선택 — 최애 몰입 붙일 때):**

| # | 작업 | 위치 | 규모 |
|---|---|---|---|
| 5 | 서버: 웹게임용 최애 조회 API 신설 | `api/users/` (recommend 모듈 참고) | S |
| 6 | 서버: `web:game:read` 스코프 추가 | `api/users/facades.py:164` | XS |

애돌 본체 앱 화면/DB/랭킹/하트 경제 **변경 없음.**

### ⚠️ 확정 필요한 것 — "JWT를 어디서 검증하나" (작업 #3)
핵심 실무 결정은 **드림그룹의 백엔드 구조**입니다. 드림그룹 프로토는 현재 **Firebase(BaaS) 기반**이라 자체 검증 서버가 얇습니다. 두 갈래:
- **(a) 드림그룹이 자체 서버/Cloud Function 있음** → C:Z9 SSO 방식(RS256 + `GET /api/v1/auths/jwks/` 공개키 검증)을 그대로 복제. 키 공유 없이 안전. **권장.**
- **(b) 순수 프론트(정적 웹앱)만** → 토큰을 검증할 곳이 없음. 이 경우 애돌 서버에 "이 토큰 유효한가?" 확인용 경량 엔드포인트가 하나 필요하거나, Firebase Function에서 JWKS로 검증. 이 갈래는 개발자와 방식 합의 필요.

---

## 6. 지켜야 할 선 (설계 가드레일)

- 최애 정보는 **읽기만.** 본체 하트·투표·랭킹에 **쓰기 금지** (앱인앱 격리).
- 실존 아이돌 **사진 원본은 변형 금지** (이펙트는 카드 프레임/배경에만). "유저 창작 콘텐츠" 명목은 시스템이 이미지를 가공하는 순간 끊길 수 있음.
- 실존 아이돌 **능력치 수치화·서열화 금지** (명예/팬덤 리스크, UGC로 안 덮임).
- 토큰은 네이티브↔서버 HTTPS 세션 교환에만 사용. URL·postMessage·웹 저장소 전달 금지.

---

## 7. 개발자에게 확인할 질문

1. **(핵심)** 앱인앱 웹게임의 로그인 검증은 **기존 `web_token`(JWT) 재활용**으로 가면 될까요? 그리고 드림그룹이 이 토큰을 **어디서 검증**하는 게 좋을까요? — (a) 자체 Cloud Function에서 JWKS 공개검증(C:Z9 방식 복제) vs (b) 애돌 서버에 토큰확인용 엔드포인트 신설.
2. 웹토큰 TTL 1시간인데, 게임 세션이 길어질 때 **갱신(refresh)** 은 어떻게 처리할까요?
3. **(나중에)** 최애 정보 연동을 붙일 때는 `@auth_jwt` 신규 엔드포인트 + `web:game:read` 스코프로 여는 걸로 괜찮을까요? — MVP엔 불필요, 옵션.
