# 육성 코어 루프 벤치마크 — 드림그룹 이식용 레퍼런스

> 목적: 학원아이돌마스터(학마스)·우마무스메의 "육성(트레이닝) 코어 루프"를 시스템·비주얼 양면으로 해부해서,
> 우리 웹게임(순수 HTML/JS 단일페이지, 서버X, Live2D 불가, 실존 아이돌 얼굴 사진, 스탯 6종)에 이식 가능한
> 구체적 설계안까지 뽑는다.
>
> ⚠️ 표기 규칙: 웹 검색·위키·공략으로 **확인된 사실**은 그대로, 내가 이 게임들을 알고 채운 부분은 `[추정]`,
> 수치가 버전·난이도에 따라 달라지는 건 `[버전차]`로 표시했다. 학마스는 라이브 이후 밸런스 패치가 잦아
> 절대수치보다 **구조와 설계 의도**를 봐야 한다.

---

## 0. 세 줄 요약 (바쁘면 여기만)

1. **학마스 = "덱빌딩 로그라이트 + 시험이라는 클라이맥스"**. 한 판(프로듀스)은 약 15~18주. 매 주 레슨/휴식/상담 중 택1로 스킬카드를 모으고 강화하고, **중간시험(7주경)·최종시험(17주경)이라는 미니 카드배틀**에서 라이벌과 점수로 겨룬다. 재미의 3축 = ① 손패 RNG로 매 턴 판단이 다름 ② 서포트카드/P아이템 조합으로 **빌드가 발산**(호조축 vs 호인상축) ③ **시험 = 자원 다 쏟는 긴장의 방출**.
2. **우마무스메 = "약 70턴 스케줄 관리 + 목표 레이스 관문"**. 카드배틀은 없지만 **매 턴 5종 트레이닝/휴식/외출/양호실/레이스** 중 택1, **체력·컨디션·서포트덱 우정(무지개)트레이닝**이 핵심 긴장. 목표 레이스 실패 = 육성 즉시 종료(로그라이트 리셋감).
3. **우리 이식 결론(선요약)**: 학마스식 카드배틀을 그대로 옮기면 웹/모바일에 과하다. **12턴 내외로 압축 + 손패 3~5장 + 자원 2종(체력·집중) + 오디션은 "3턴 점수 방출" 미니게임**. 실사 얼굴은 Live2D 대신 **CSS 트랜스폼(호흡/틸트/줌인)·표정 컷 3~4종·리액션 말풍선**으로 "살아있는 느낌"을 만든다. 상세 수치 초안은 §7.

---

## 1. 학원아이돌마스터 (학마스 / Gakumas, QualiArts, 2024) ★핵심

> 장르 정의(위키): "덱빌딩 요소가 있는 아이돌 육성 시뮬레이터", 사실상 **로그라이트 덱빌딩**.
> 한 판을 "프로듀스"라 부르고, 매판 스킬카드 덱을 새로 쌓아 시험을 통과시키는 구조.

### 1.1 한 판(프로듀스)의 주(週)/턴 구조

- **한 판 = 정해진 주(週) 스케줄**. 매 주가 1턴이고, 그 주에 할 행동 하나를 고른다.
- **총 기간**: 초급 시나리오(하지메/Hajime) 기준 대략 **15~18주**. 확인된 앵커: **중간시험 직전 = 약 7주차**, **최종시험 직전 = 약 17주차** (공략 기준). [버전차] 시나리오(하지메/N.I.A 등)마다 총 길이·시험 시점이 다르다.
- 시간축이 **가시적 주간 캘린더/보드**로 깔려 있고, 각 칸에 그 주가 어떤 종류(일반 레슨 주 / SP레슨 주 / 시험 주 / 상담 주 / 활동 주)인지 아이콘으로 예고된다. → 플레이어가 **몇 주 뒤 뭘 할지 미리 계획**(자원 배분)하게 만드는 게 핵심.
- 흐름: `프로듀스 시작(덱·서포트 세팅) → 주차별 행동 반복 → 중간시험 → 다시 주차 반복 → 최종시험 → 엔딩 라이브 → 메모리(육성 산출물) 추출`.

### 1.2 매 턴(주) 선택지

각 주에 아래 중 하나를 고른다. 그 주가 어떤 칸이냐에 따라 선택지가 조금씩 다르다.

| 행동 | 효과 | 비용/리스크 |
|---|---|---|
| **레슨 (Vocal / Dance / Visual)** | 해당 파라미터를 올리는 **미니 카드배틀 1세션**. 손패에서 스킬카드를 내 점수를 쌓고, 목표치 도달 시 파라미터 상승 + (때때로) 스킬카드 획득 | 카드 사용 시 **체력 소모**. 무리하면 시험 전 체력 고갈 |
| **SP 레슨(특별 레슨)** | 특정 주에 등장하는 강화 레슨. 획득량↑ + **서포트카드가 붙어 우정/보너스** 발생, 좋은 카드·P아이템 획득 확률↑ | 서포트카드 배치·컨디션에 따라 가치가 요동 |
| **활동(お仕事/일)** | 파라미터 소폭 + 자원/보상(P포인트, 팬 등). 레슨보다 안전 | 성장 효율은 레슨보다 낮음 |
| **휴식(휴양)** | **체력 약 70% 회복(내림)**. 아무것도 안 하는 것보다 항상 이득 | 그 주에 성장 없음(기회비용) |
| **상담(相談/컨설팅)** | **덱 정비의 핵심**: 카드 강화(레벨업), **카드 삭제(덱 압축)**, P아이템·P드링크 획득/교환 | 횟수가 한정 — 보통 **중간 전 1회, 최종 전 1회**가 실질적 정비 창구 |
| **외출/컨디션 관리** [추정] | 컨디션(호조) 정비, 랜덤 커뮤 이벤트 | — |

> 공략 핵심: "**덱 정비 대부분은 최종시험 직전 상담에서 끝난다. 실질적으로 중간 직전 1회 특별지도밖에 없다**" → 자원(상담 횟수)이 극히 귀해서 **언제 무엇을 압축/강화하느냐**가 실력의 상당 부분.

### 1.3 스킬 카드 시스템 (게임의 심장)

- **덱 → 손패 드로우 → 사용 → 버림더미 → 리셔플**의 정석 덱빌딩. 레슨/시험이 시작되면 매 턴 손패를 몇 장 뽑아, 그 중 하나를 내고, 카드 효과가 발동한다.
- **카드 종류**:
  - **액티브 카드(A)**: 점수(파라미터)를 직접 내는 공격형. 카드 하단 중앙에 `A` 표기.
  - **멘탈 카드(M)**: 버프/자원(집중·호인상·체력회복·드로우 등)을 까는 지원형. 하단에 `M` 표기.
- **카드 사용 = 체력(스태미나) 소모**. 카드마다 소모 체력이 다르고, **상태에 따라 소모가 달라짐**: 확인된 예 — "강기(강한 각오) 상태에서 쓰면 체력 소모가 무려 14, 온존(아껴둠) 상태에서 써야 부담이 적다." → **체력은 판 전체를 관통하는 최대 제약 자원**. 최종시험 전 체력이 8 미만이면 "거의 게임오버"라는 공략이 있을 정도.
- **에너지/자원 축(플랜별로 다름)** — 학마스의 빌드 발산 근원:
  - **센스(Sense) 플랜**: **호조(好調)/절호조(絶好調)** + **집중(集中)**. 호조는 활성 동안 파라미터 획득 +50%. 집중은 스택형 추가 점수. → "버프 쌓고 한 방에 터뜨리는" 축.
  - **로직(Logic) 플랜**: **호인상(好印象)** + **의욕(모티베이션)**. 호인상은 매 턴 지속 점수, 의욕은 획득 증폭. → "꾸준히 굴리는" 축.
  - (신규 어노말리 계열 등 추가 축도 있음 [버전차])
- **카드 효과 배경색 = 부가 규칙**(확인): 붉은색=**트러블/페널티**(강카드에 붙는 대가), 주황색=**지속 효과**(조건마다 계속 발동), 초록색=**성장 효과**(조건 충족마다 그 레슨 동안 성능이 변함).
- **카드 강화(레벨업)**: 상담/보상으로 카드를 `+` 버전으로 승격. 예: 기초 카드가 강화되면 위력·부가효과가 붙음. 40레벨 이상에서 얻는 **"스킬카드 체인지" 효과**로 기초카드가 상위 무지개 카드로 변신하기도("기초 표현"→"내셔널 아이돌").
- **카드 삭제 = 덱 압축**: 약한 카드를 빼서 **좋은 카드가 손에 잡힐 확률↑**. 로그라이트 덱빌딩의 정석(슬레이 더 스파이어의 카드 제거와 동일 철학). 시험(선발시험) 종료 후 **랜덤으로 2장씩 삭제**되는 이벤트도 있어, 압축이 자동/수동 양쪽으로 일어난다.

### 1.4 시험 (중간·최종 오디션) = 클라이맥스

- **구조**: 시험 자체가 **독립된 턴제 카드배틀**. 정해진 턴 수 안에 손패로 점수를 최대한 쌓는다. **라이벌 2~3명과 실시간 점수 경쟁**처럼 연출되고, **최종 순위(1/2/3위)**가 산출된다. [추정: 턴 수는 시험마다 고정, 최종이 더 김]
- **파라미터 Vo/Da/Vi**: 육성으로 쌓은 세 파라미터가 시험의 시작 점수/배율 기반이 됨. 최종 순위·점수가 합쳐져 **프로듀스 랭크(F ~ S5)** 산출.
- **점수 → 랭크 환산(확인, 하지메 기준)**:
  - **순위 점수**: 1위 = 1,700 / 2위 = 900 / 3위 = 500 (라이벌 대비 등수)
  - **파라미터 점수**: Vo+Da+Vi 합(+순위 보너스 +30/+20/+10) × **2.3** (레전드 난이도는 ×2.1, 보너스 +160/+80/+40)
  - **시험 점수 환산**: 체감 점수를 랭크점수로 바꿀 때 **체감(逓減) 구간** — 5,000점까지 0.3/점, 이후 0.15 → 0.08 → 0.04 → 0.02 → 0.01로 점점 효율이 깎임. → **"많이 낼수록 한계효용 감소"** = 무한 스케일 방지.
  - 예시 공략: 특정 A+ 목표에 "최종시험 15,288점 이상 필요", "중간은 약 1,700점이면 통과 확정".
- **스킬체크/긴장 연출**: 시험 중 "이번 턴은 어떤 속성이 요구되는지"가 표시되어, **강한 피니시 카드를 요구 속성이 맞는 턴에 터뜨려야** 효율이 극대화(엇나가면 위력 반감). → "언제 방출하느냐"의 타이밍 긴장.
- 최종시험 뒤 **엔딩 라이브(공연)**로 카타르시스 제공. (성장의 '비포→애프터'를 보여주는 감정 페이오프)

### 1.5 P아이템 / P드링크 / 서포트 카드

- **P아이템**: 판 내내 지속되는 **패시브 유물**(로그라이트의 렐릭). 예: "특정 조건마다 체력회복", "호조 부족을 보완하는 티셔츠 아이템". 프로듀스 시작 시 아이돌 **고유 P아이템**이 하나 붙어 그 판의 플레이 스타일을 규정.
- **P드링크**: 판 중 소모하는 **1회성 포션**. 예: "우롱차 = 사실상 체력 7 회복 = 갓템". 시험 직전 체력 관리·버프에 쓰임.
- **서포트 카드(가챠 대상)**: 프로듀스 시작 전 **덱(서포트 6장 등)을 편성**. 서포트는 SP레슨 등장률/성장률↑, 고유 스킬카드·P아이템을 판에 **투입**해준다. → **"이 판에 어떤 카드 풀이 깔리느냐"를 서포트 편성이 결정** = 가챠 수익 모델이자 빌드 다양성의 원천.
- **고유 스킬카드 + 고유 P아이템**의 조합이 각 P아이돌의 개성(플레이 스타일)을 만든다.
- **메모리(육성 산출물)**: 판이 끝나면 그 아이돌의 성과를 "메모리"로 추출 → 다음 판·콘테스트·"아이돌의 길" 콘텐츠에 계승(우마의 인자 계승과 유사한 메타 성장). ⚠️ 우리 게임 메모: **멘토/인자 계승은 접기로 결정**(육성카드가 이미 그 역할).

### 1.6 컨디션 / 랜덤성 / 트렌드

- **컨디션(호조/부진)**: 판 도중 상태 버프/디버프. 호조 유지가 획득 효율에 직결.
- **랜덤 커뮤(コミュ) 이벤트**: 주차 중간중간 대화 이벤트가 터져 선택지에 따라 보상/버프/스토리. → **매판 다른 서사감** + 캐릭터 애착.
- **트렌드/특화**: 그 판/시즌에 유리한 속성·유형이 있어, 덱을 거기 맞추면 유리. "단일 속성 특화는 매우 불안정, 적/청 이중 속성 덱이 안정적" 같은 메타 조언 존재.

### 1.7 "왜 매판 다르고 재밌는가" (이식의 핵심 통찰)

1. **손패 RNG**: 같은 덱이어도 매 턴 뽑히는 카드가 달라, "지금 이 손패에서 최선"을 매번 새로 판단. (전술적 즉흥성)
2. **빌드 발산**: 센스(호조·집중) vs 로직(호인상·의욕) + 서포트/P아이템 조합으로 **판마다 완전히 다른 엔진**을 조립. (전략적 정체성)
3. **자원의 삼중 긴장**: **체력(쓸수록 줄고 시험 전 바닥나면 죽음)** × **상담 횟수(덱 정비 기회 극소)** × **주차(시간)**. 세 자원이 서로 물려 매 선택이 아프다.
4. **시험 = 방출의 카타르시스**: 15주 동안 아낀 자원을 시험 3~수 턴에 **한 방에 쏟는** 구조. "쌓기 → 터뜨리기"의 리듬.
5. **비포→애프터 서사**: 초라했던 아이돌이 엔딩 라이브에서 빛나는 감정 페이오프. (우리 게임의 **Day1 고스트 레이더**가 겨냥한 바로 그 지점 — 이미 도입함)

---

## 2. 우마무스메 프리티 더비 (Cygames)

> 카드배틀은 없지만 **"제한된 턴 안의 스케줄 최적화 + 관문(목표 레이스)"** 이라는 로그라이트 육성의 또 다른 정석.

### 2.1 육성 기간·턴 구조

- **1 육성 ≈ 70~72턴** (주니어 → 클래식 → 시니어, 각 학년이 여러 달×전·후반 턴으로 쪼개짐).
- 매 턴 = 반달(半月) 하나. 그 턴에 할 행동 하나를 고른다.
- 중간중간 **목표 레이스(관문)**가 박혀 있고, **목표 레이스에서 지정 등수 안에 못 들면 육성 즉시 종료(실패)**. → 로그라이트의 "죽으면 리셋" 긴장.

### 2.2 매 턴 행동

| 행동 | 효과 | 비용 |
|---|---|---|
| **트레이닝 5종** (스피드/스태미나/파워/근성/지능) | 해당 스탯 상승 + 스킬Pt. 레벨업 시 시설 강화 | **체력 소모** (지능 트레이닝만 **체력 소모 없음** + 소폭 회복, 특이) |
| **휴식(휴양)** | 체력 회복 **30 / 50 / 70** (보통 50). | 성장 없음 |
| **외출/놀기(お出かけ)** | **컨디션(의욕) 상승**. 노래방 등은 컨디션 2단계↑지만 체력 회복 없음 | 성장 없음 |
| **양호실** | 배드 컨디션(상태이상) 치료 | 성장 없음 |
| **레이스 출주** | 상금·팬·스킬Pt·소질↑. 목표 레이스는 필수 | 체력 소모, 패배 리스크 |
| **스킬 습득** | 스킬Pt로 스킬 구매. **턴 소모 안 함**(언제든) | 스킬Pt |

- **체력이 낮을 때 트레이닝 강행 = 실패 확률 발생**(부상·성장 실패). → 체력 관리가 곧 리스크 관리.

### 2.3 컨디션(의욕) & 스탯

- 컨디션(やる気) **5단계**: 최상 / 좋음 / 보통 / 나쁨 / 최악 [추정 라벨]. **높을수록 트레이닝 획득량↑, 레이스 성능↑**. 초반에 빨리 '최상' 찍고 끝까지 유지가 정석.
- 5스탯 = 스피드/스태미나/파워/근성/지능 + **스킬Pt**. 거리·각질(전법)에 따라 스탯 우선순위가 다름("완주할 스태미나 > 최고 스피드 > 따라잡을 파워 > 지능 400 > 근성").

### 2.4 서포트 카드 덱 & 우정 트레이닝 (학마스 서포트와 대응)

- **서포트 덱 6장** (본인 5 + 렌탈 1). 각 서포트는 특정 트레이닝 시설에 나타나 **스탯 보너스 + 스킬 힌트 + 이벤트** 제공.
- **우정(친애도) 게이지**: 같이 트레이닝할수록 참 → 마지막 바(주황)에서 **우정 보너스** 해금.
- **무지개(레인보우) 트레이닝**: 친애 만렙 서포트가 자기 타입 시설에 뜨면 **무지개 오라 + 대폭 획득**. **한 시설에 무지개 서포트가 여러 장 겹칠수록 배수**로 폭발(후반 4스피드덱은 한 번에 스피드 +100까지).
- **어깨(복수 배치) 보너스**: 한 트레이닝에 서포트가 몰릴수록 획득↑ + 친애 동시 상승. → "어디에 몇 명 몰렸나"를 보고 트레이닝 선택.
- **스킬 힌트**: 서포트가 트레이닝/이벤트에서 스킬 힌트를 줘 스킬을 싸게/강하게 습득.

### 2.5 목표 레이스 / 스킬체크 / 시나리오

- **목표 레이스 = 관문**. 지정 등수 실패 시 육성 종료. 레이스는 직접 조작이 아니라 **쌓은 스탯 + 습득 스킬 발동**의 결과 시뮬레이션(스킬이 적재적소 발동하면 역전).
- **시나리오 기믹**: 육성 시나리오(URA/아오하루/그랜드마스터즈 등)마다 **고유 자원·미니게임**이 얹혀 매판 전략이 달라짐 → 학마스의 "플랜/시나리오"와 같은 역할.

### 2.6 학마스와의 공통 DNA

- 둘 다 **한 판 = 제한 자원(체력·턴)** + **덱 편성(서포트)** + **관문(시험/목표 레이스)** + **판 종료 후 메타 계승**.
- 차이: 학마스는 **판 내부에 카드배틀 레이어**를 하나 더 얹어 전술 깊이↑ / 우마는 **스케줄 최적화 + 확률 관리**에 집중.

---

## 3. 다른 아이돌게임의 "성장" 처리 (간단)

- **프로젝트 세카이(프로세카) / 뱅드림(걸즈밴드파티)**: **이건 육성 시뮬이 아니라 리듬게임 + 카드 수집**. "성장"은 캐릭터를 키우는 게 아니라 **카드를 키우는** 것 — 카드 레벨업, **각성(일러스트 변화)**, 스킬 레벨, 마스터 랭크(★), 캐릭터 랭크, 인연(에리어) 랭크, 에리어 아이템(밴드/캐릭별 패시브 버프) 강화. → **"성장 = 수치 쌓기 대시보드"** 모델. 우리 게임의 로그라이트 육성과는 결이 다르지만, **수집·강화의 도파민 루프**(도감·랭크·별)는 참고할 만함. (우리 게임의 카드도감·NEW뱃지·정렬이 이미 이 계열)
- 시사점: **육성 코어(로그라이트)는 학마스/우마에서, 컬렉션 메타 루프는 프로세카/뱅드림에서** 벤치마크하는 게 맞다.

---

## 4. 비주얼 / UX 벤치마킹 — 트레이닝 화면

> 스크린샷을 직접 열어보진 못했다(위키 이미지 접근 차단). 아래는 공개 플레이 영상·스크린샷·UI 관습에 대한 **구조적 서술**이며, 세부 픽셀 배치는 `[추정]`.

### 4.1 학마스 트레이닝/시험 화면 (덱빌딩 배틀 레이아웃)

- **캐릭터**: 화면 **상단 2/3를 Live2D 전신/반신**이 차지. 카드를 낼 때마다 **모션·표정·컷인**이 크게 터진다. 배경은 연습실/무대. → "캐릭터가 주인공, UI는 하단 프레임".
- **HUD(자원)**:
  - **좌상단**: 체력바(스태미나) — 판 전체 최대 제약이라 항상 눈에 보이게.
  - **상단 중앙/좌**: 목표 점수 / 현재 점수(시험 시) — 라이벌과의 **실시간 순위 바** [추정].
  - **버프 아이콘 열**: 호조/집중/호인상/의욕 등 현재 스택을 아이콘+숫자로 캐릭터 근처에 표시.
- **손패 카드**: 화면 **하단에 부채꼴(fan)로 3~5장** 펼침. 카드에 이름·소모 체력(좌상 배지)·효과 아이콘·`A`/`M` 표기·레어도 프레임. 사용 = **위로 드래그/탭 → 카드가 중앙으로 날아가 발동 이펙트**. 낼 수 없는 카드(체력 부족)는 어둡게.
- **연출 언어**: **속성 색 = Vo 빨강 / Da 파랑 / Vi 노랑**(확인). 점수가 오를 때 **숫자 팝(+1,240)**이 튀고, 콤보·크리티컬 시 **화면 플래시 + 셰이크 + 컷인**. 시험 마지막 턴 "피니시"는 슬로우+대형 이펙트.
- **결과/랭크 리빌**: 시험 후 **F~S 랭크가 도장 찍히듯 쾅**, 이어서 **엔딩 라이브(3D MV)**로 감정 방출. 파라미터는 **레이더/막대가 차오르는 애니메이션**으로 비포→애프터.

### 4.2 우마무스메 트레이닝 화면 (스케줄 보드 레이아웃)

- **캐릭터**: 화면 **중앙~우측에 Live2D 반신**, 컨디션에 따라 표정·포즈 변화. 상단에 목표 배너.
- **HUD**: **체력바(무지개색, 상단)** + 컨디션 아이콘 + 5스탯 수치(중앙 하단 표) + 스킬Pt + 날짜/턴 카운터.
- **행동 선택**: 화면 **하단에 5개 트레이닝 버튼 + 휴식/외출/양호실/레이스**. **각 트레이닝 버튼 위에 지금 그 시설에 있는 서포트 아이콘들**이 떠 있음 — **무지개 오라/느낌표(스킬힌트)/친애 게이지**로 "여기 누르면 이득"을 한눈에 표시. → **정보를 버튼에 직접 얹는** 설계가 핵심.
- **연출**: 무지개 트레이닝은 **화면 전체 무지개 파티클 + 스탯 대폭 팝**. 레이스는 별도 3D 중계 화면 + 실황.

### 4.3 우리가 훔쳐올 UI 원칙 (요약)

1. **캐릭터를 화면의 주인공으로** (상단 대부분). 자원/버튼은 하단 프레임.
2. **손패는 하단 부채꼴**, 카드에 소모자원 배지 + 색 프레임(레어도/속성).
3. **정보를 액션 버튼에 얹기**(우마식): "이거 누르면 뭐가 좋은지"를 버튼 위 뱃지/오라로.
4. **점수 팝·플래시·셰이크·콤보** = 손맛. (우리 게임에 이미 일부 있음)
5. **결과 = 랭크 도장 + 비포/애프터 레이더 + 짧은 라이브 컷** = 카타르시스.
6. **속성 컬러 코딩** 일관 유지.

---

## 5. 우리 게임 현황 스냅샷 (이식 대상 파악)

- 스택: 순수 HTML/JS **단일 페이지**(`index.html` ~480KB), 서버X, Firebase는 경쟁전 리더보드만. **Live2D 불가**, 캐릭터 = **실존 아이돌 얼굴 사진**.
- **스탯 6종 유지 필수**: **발성 / 리듬감 / 체력 / 스타일 / 화술 / 창조** (STATS/STATNM/st_* 기준. ⚠️ 옛 메모의 "보컬·랩·댄스…" 아님).
- 이미 있는 것: 24턴 풀런 육성 + 관문(오디션) 4단계, 육성카드(수집·조합·별머지·레어도 N~UR), 결과 레이더 + **Day1 고스트**(비포/애프터 이미 구현), 편성 9포지션·경쟁전(마이크 재화·승점 티어), 손맛 팩(셰이크·햅틱·콤보·+N 팝).
- 즉, **뼈대는 우마식(턴 스케줄 + 관문)에 가깝고, 카드는 "수집·강화 메타"로만 존재**. → **비어 있는 곳 = 학마스식 "판 내부 전술 레이어(손패로 매 턴 판단하는 재미)"**. 이걸 이식하는 게 이 문서의 최종 타깃.

---

## 6. 이식 설계 권고 ★가장 중요

### 6.0 큰 방향: "학마스를 3분의 1로 접기"

학마스의 재미(손패 RNG·빌드 발산·자원 긴장·시험 방출)는 **웹 단일페이지에서도 재현 가능**하다. 단, 학마스의 15주×수십 카드×정밀 수치는 모바일 웹·비개발자 PM 운영엔 과하다. **핵심 3축만 남기고 접는다**:

- **접는 것**: 판 길이(15주→약 12턴), 손패 크기(5+→3~5), 자원 종류(체력·집중·호인상·의욕 4축 → **2축**), 상담/P드링크 세분화, 라이브 3D.
- **남기는 것**: ① 매 턴 손패에서 고르는 전술 ② 빌드 발산(2~3 엔진) ③ 자원 긴장(체력) ④ 오디션 = 방출의 카타르시스 ⑤ 비포/애프터 서사.

### 6.1 턴/판 구조 (수치 초안)

- **한 판 = 12턴**(현행 24턴은 길다는 밸런스 신호[내부 시뮬에서 등급 E 도배·난이도 역전 관측]와도 맞물림 — 압축이 정답에 가까움).
- **관문(오디션) 3회**: **4턴차 = 예선 / 8턴차 = 준결승 / 12턴차 = 데뷔 오디션(최종)**. (학마스의 중간·최종 2관문을 3관문으로. 우리 게임의 "예선/생존전/파이널" 서사와 정합.)
- 각 턴 아이콘으로 **다음 관문까지 D-N**·그 턴 종류(일반/특훈/상담)를 예고 → 계획성.

### 6.2 매 턴 행동 (6스탯에 맞춤)

턴마다 아래 중 **하나** 선택:

| 행동 | 효과 (초안) | 자원 |
|---|---|---|
| **레슨(6스탯 중 택1)**: 발성/리듬감/체력/스타일/화술/창조 | 그 스탯 대상 **미니 카드 세션**(§6.4). 손패로 점수 쌓아 스탯 상승 | 카드마다 **체력** 소모 |
| **특훈(SP레슨)** | 특정 턴 등장. 획득 ×1.5 + 카드/버프 드랍 확률↑ | 체력 소모 큼 |
| **휴식** | 체력 **+65%**(내림) | 성장 없음 |
| **상담** | **카드 1장 강화 or 1장 삭제(덱 압축) or 유물 획득** 중 택1. 판당 **2~3회 한정** | 무료지만 횟수 희소 |
| **교류(커뮤)** | 랜덤 이벤트(대사풀 활용) + 소폭 버프/컨디션. **우리 게임 "유대커뮤" 계획과 직결** | 성장 없음 |

> 6스탯 전부에 레슨을 두면 선택지가 6개로 넓어 **빌드 특화**(2~3스탯만 집중)가 자연 발생 = 발산의 씨앗.

### 6.3 자원 = 2축으로 압축

1. **체력(스태미나)** — 시작 30 전후, 카드 사용 시 1~7 소모, 최대 40 캡. **관문 직전 바닥나면 위험**(학마스 그대로). 휴식/드링크로 회복.
2. **집중(컨디션)** — 이 게임 유일한 버프 스택 자원. **집중 스택만큼 카드 점수 +N%** (예: 스택당 +10%, 최대 5스택 = +50%). 특정 카드가 집중을 쌓고, 다른 카드가 집중을 소모해 대박을 터뜨림. → **"쌓고 터뜨리는" 리듬 1축으로 압축**.

> 학마스의 호조·집중·호인상·의욕 4축을 **집중 1축**으로 접되, **빌드 발산은 카드 계열(§6.5)로** 만든다.

### 6.4 카드 세션(레슨/오디션 공통 미니게임)

- **손패 크기 4장**, 덱은 육성카드+기본카드 합쳐 10~16장.
- 한 세션 = **3턴**(레슨) / 오디션은 **3~4턴**. 매 턴 손패 4장 중 **1장 사용 → 버림 → 1장 드로우**.
- 카드 = **{ 소모체력, 계열, 효과 }**. 화면 하단 부채꼴, 소모체력 배지 + 6스탯 색 프레임.
- 점수 = `기본치 × (1 + 집중스택×0.1) × 컨디션보정`. 목표치 도달 시 스탯 상승.

### 6.5 빌드 발산 = 3계열 카드 엔진

가챠 없으니 **육성카드 조합 = 서포트카드 대체**. 카드를 3계열로 태깅해 판마다 다른 엔진을 유도:

- **폭발형(집중)**: 집중을 쌓고 한 방에 소모(발성/스타일 특화와 시너지). "쌓기→터뜨리기".
- **지속형(리듬)**: 매 턴 소량 점수 + 체력 효율(체력/리듬감 특화). "안정적 누적".
- **콤보형(창조/화술)**: 카드 연쇄·추가 드로우·손패 조작. "손패 엔진 돌리기".
- 판 시작 시 **편성한 육성카드 계열 비중**에 따라 엔진이 갈리고, **상담에서 압축**해 순수도를 높인다. → 학마스의 센스/로직 발산을 **로컬·무가챠로 재현**.

### 6.6 오디션(관문) 연출 = 방출의 카타르시스

- 관문 = **3~4턴 카드 세션**인데 **라이벌 1~2명(RIVAL_PEOPLE 활용)과 실시간 점수 경쟁 바**. 목표점수 + 라이벌 점수 두 줄.
- **턴별 요구 속성 표시**(학마스식): "이번 턴 ★리듬감 우대(×1.5)" → 그 턴에 맞는 계열 카드를 터뜨리면 대박. **타이밍 긴장**.
- 결과 = **순위(1/2/3위) + 등급 도장(쾅) + 비포/애프터 레이더(Day1 고스트, 이미 있음) + 짧은 "라이브" 컷**(실사 사진 슬라이드쇼 + 색종이 + BGM 후렴).
- 점수→등급은 **체감 구간**(학마스식): 일정 점수 넘으면 효율 감소 → 과성장 방지, 이미 내부 밸런스 시뮬과 정합.

### 6.7 컨디션·이벤트·트렌드

- **컨디션 3단계**(최상/보통/부진)로 축소 — 교류/휴식으로 관리, 획득 ±20%.
- **랜덤 커뮤 이벤트**: 기존 돌발이벤트 + 대사풀 확장(메모의 다음 착수 항목)과 통합. 선택지 → 카드/집중/체력 보상.
- **트렌드**: 경쟁전 시즌 트렌드(이미 있음)를 육성에도 연결 — "이번 시즌 ★스타일 우대" → 관문에서 해당 계열 가산. **경쟁전-육성 루프 연결**로 재플레이 동기.

### 6.8 수치 초안 한 장 요약 (튜닝 시작점 — 전부 초안, 실플레이로 최종)

```
판 길이        : 12턴
관문           : 4턴(예선) / 8턴(준결) / 12턴(데뷔 최종)
자원           : 체력(시작 30, 최대 40, 카드 1~7 소모) / 집중(스택당 +10%, 최대 +50%)
휴식 회복      : +65%(내림)
상담 횟수      : 판당 3회 (강화/삭제/유물 택1)
손패           : 4장, 덱 10~16장
레슨 세션      : 3턴 / 오디션 세션 : 3~4턴
카드 계열      : 폭발(집중) / 지속(리듬) / 콤보(손패)
점수식         : 기본 × (1+집중×0.1) × 컨디션(0.8~1.2)
등급 환산      : 체감 구간(고득점 효율 감소) — 기존 시뮬 상수 재사용
컨디션         : 3단계(최상/보통/부진), 획득 ±20%
```

### 6.9 단계적 도입 로드맵 (리스크 최소화)

1. **1차(MVP)**: 기존 24턴을 12턴으로 접고, **레슨을 "카드 3장 중 1장 선택" 초간이 세션**으로 교체(집중 1축만). 관문은 기존 판정 유지. → 전술감만 먼저 검증.
2. **2차**: 카드 3계열 + 상담(압축/강화) + 오디션 요구속성/라이벌바. → 빌드 발산·긴장 완성.
3. **3차**: 커뮤 이벤트·트렌드 연결·라이브 컷 연출 폴리시.
- 각 단계 **실플레이 체감으로 수치 최종**(내부 원칙: 완료=검증 후, 시뮬은 실코드 상수 참조).

---

## 7. Live2D 없이 "살아있는 실사 얼굴" 만들기 (UI 아이디어)

Live2D를 못 쓰고 캐릭터가 **정지 얼굴 사진**이라는 게 가장 큰 비주얼 제약. 아래는 **순수 CSS/JS로 생명감**을 주는 기법들(가벼움 순):

1. **호흡 애니메이션**: 사진 컨테이너에 `transform: scale(1.0↔1.02)` + `translateY(0↔ -2px)`를 4초 ease-in-out 무한 루프. 아주 미세해도 "숨 쉬는" 착시. (이미 living-demo.html/train-demo.html 계열에서 실험 중인 걸로 보임 — 확장)
2. **패럴랙스 틸트**: 기기 자이로/포인터 위치에 따라 사진을 ±3° 기울이고 배경 레이어를 반대로 → 입체감. (`.tilt` 이미 있음)
3. **표정 컷 교체(2~3장)**: 한 아이돌당 **기본/미소/놀람** 정도 크롭 3장을 준비해, **크리티컬·콤보·실패 때 순간 교체**(80ms 크로스페이드). 사진 1장만 있으면 **밝기/채도/블룸 필터**로 유사 효과(성공=화사, 실패=탈색).
4. **줌·리프레이밍**: 대사·리액션 때 얼굴로 **천천히 줌인(Ken Burns)**. 관문 피니시엔 **급줌 + 플래시**.
5. **리액션 말풍선(sayLineOf 확장)**: 카드 성공/콤보/집중만렙 순간 캐릭터 위 말풍선("가자!", "이거야!"). 대사풀 확장 계획과 직결 — **정지 사진이라도 "반응하는 인격"**으로 느껴지게.
6. **파티클·오라 오버레이**: 집중 스택 쌓이면 얼굴 주변에 **글로우/입자**, 무지개(대박) 턴엔 **홀로 스윕**(카드 컴포넌트의 홀로 효과 재사용).
7. **컷인 프레임**: 강카드 사용 시 사진을 **대각 컷인 프레임**에 담아 슬라이드 인 + 스피드라인 배경(만화식). Live2D 모션의 저비용 대체.
8. **사진 크롭 표준화**: 실사라 구도가 제각각 → 기존 원형/크롭 모달로 **눈높이·얼굴 크기 정규화**해야 위 연출이 일관되게 먹힘.

> 핵심: Live2D의 "관절 움직임"은 못 이겨도, **호흡+틸트+표정컷+말풍선+파티클** 5종을 조합하면 정지 실사도 "살아있다"는 인상을 준다. 비용 대비 효과 1순위 = **호흡 루프 + 리액션 말풍선 + 표정 밝기 필터 교체**.

---

## 8. 출처 (주요)

- 학원 아이돌 마스터/프로듀스, 스킬카드, 덱 — 나무위키 (한/영판, 직접 fetch는 차단되어 검색 스니펫으로 확인)
- Gakuen Idolmaster — Wikipedia: https://en.wikipedia.org/wiki/Gakuen_Idolmaster
- Gakuen iDOLM@STER (game) — project-imas wiki
- Gakumas Tools (produce rank calculator, 점수 환산 수치) — https://gktools.ris.moe/en
- Gakumas 공략(zool neocities / note.com elzam) — 주차·체력·상담 타이밍·점수 목표
- 우마무스메 Beginner Guide — GameTora: https://gametora.com/umamusume/beginners-guide
- Umamusume Wiki (Career Mode / Stat Training) — https://umamusu.wiki
- 우마무스메 시스템/트레이닝 — 나무위키
- 프로세카/뱅드림 — 나무위키(성장=카드 강화/각성 계열 확인)

> ⚠️ 학마스 절대 수치(주차·점수·환산율)는 **하지메 시나리오/특정 시점 기준**이며 패치·시나리오별로 변동. 이식엔 수치보다 **구조**를 참조하고, 우리 수치는 §6.8을 시작점으로 **실플레이 체감으로 최종**할 것.
